#configure.container{ :style => 'display:none;' }
  .top-links
    .pull-left
      %button.btn.btn-warning{ :type => 'button', 'data-logout' => 'true' } Logout
    .clearfix

  .well
    %h2
      Configure
      %small#configure_email_placeholder

    .pushed-down
      %h4 Master Password
      %p The Master Password is the only password you will need to remember. It is used to decrypt the rest of your individual account passwords. Enter a strong master password below.
      .alert.alert-warning
        %strong Warning:
        It is extremely important that you remember this password as it CANNOT be recovered!
      %form#configure_form
        %p= password_field_tag(:passwd, '', :placeholder => 'Password', :class => 'input-xlarge', :id => 'configure_passwd', :required => true)
        %p= password_field_tag(:passwd2, '', :placeholder => 'Confirm Password', :class => 'input-xlarge', :id => 'configure_passwd2', :required => true)
        %p= submit_tag('Set Master Password', :class => 'btn btn-primary', :id => 'configure_btn')
      = form_tag(users_path, :method => :put, :remote => true, :id => 'configure_hidden_form', :style => 'display:none;') do
        = hidden_field_tag(:new_api_key, '', :id => 'configure_hidden_new_api_key')
        = hidden_field_tag(:encrypted_data, '', :id => 'configure_hidden_encrypted_data')
        = hidden_field_tag(:schema_version, '', :id => 'configure_hidden_schema_version')
