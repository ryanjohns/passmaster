# --------------------------------------
# PostgreSQL recovery configuration file
# --------------------------------------

standby_mode             = off
restore_command          = '/usr/local/rvm/bin/wal_ruby /usr/share/postgresql-common/pg_restore %p %f'
recovery_target_timeline = 'latest'
